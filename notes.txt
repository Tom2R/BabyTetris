--> dernières modifications 24/11/2025 12h58

--> dans Game , la fonction next_state modifiait le state actuel en enlevant des lignes,
mais renvoyait le next state avec potentiellement des lignes encore full dedans...
J'ai remplacé state par new_state dans l'appel de reward

tuple_to_state --> fait (retourne un état avec la grille correspondante)
state_to_tuple --> fait (renvoie un tuple avec 1 si true et False si 0)
fils_noeud --> me donne tous les fils suivant un état (même des doublons) /done/

Loading of all states : fini pour n=2 et n=3 combinatoire raisonnable
Mais pour n=4 ça explose littéralement (trouver un moyen d'optimiser le temps
ou laisser le computer tourner toute la nuit lol) --> fait (en 2h et quelques) 
11616 combinaisons stockées

 Petit problème à régler il y a une config fausse dans le loading pour n=2, 
 mais c'est bizzare parce que la transposée fonctionnerait, je soupconne juste une
 erreur dans la construction du tuple

 Attention :  à regarder demain mais les tuples sont représentés dans l'autre sens que
 les valeurs de la grid (la transposée je pense) en soi ça marche mais lorsque on switch
 en tuple ça rend un truc faux, être averti de ne regarder que la représentation des
 états.


comment push:
--> branche principale
terminal cd le projet
         git commit -m "change choose_strategy in random player"
         git push origin main
--> branche secondaire