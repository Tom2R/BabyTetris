--> dernières modifications 28/12/2025 9h50

Ajout d'un fichier algorithms_selector pour calculer l'optimal policy du selector FACING random Player
Le save dans le fichier reverse_cheatdict/reverse_cheatdict_epsilon_lambda --> rajouter un R pour random ou O pour
optimal.

Permettre le choix de strétagie au VI selector dans le fichier players en chargeant le reverse_cheatdict

Je vais ensuite procéder à une phase de tests pour se faire affronter ce nouveau selector et le VI player

Petite modif : pour éviter circular import, dans players : from game import Piece mais pas de Piece
dans game, donc enlevé cette ligne et ça marche mieux 

Enlever "actions" des arguments de choose_strategy pour le cas précis du selector ? ne sert à rien c'est dans les attributs du jeu...

Resultats : le random selector est nul, il donne quasiment tout le temps le square (mais pas tout le temps) example de situation pour (0.01 ?)
dans le projet en image.

A faire : calculer la VI selector avec une distribution déterministe du player et non pas random. Moins dur, aucune somme à faire 
juste récupérer le dico.
Tester une heuristique pour comparer : le choix de pièce qui minimise la valeur du score obtenue par l'action (déterministe du joueur
ou en moyenne si on considère le RandomPlayer)

Tester ensuite l'évolution des droites de score par rapport à l'affrontement du Vi contre RandomSelector.

comment push:
--> branche principale
terminal cd le projet
         git add <files>
         git commit -m "change choose_strategy in random player"
         git push origin main
--> branche secondaire